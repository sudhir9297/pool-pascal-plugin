# Pool drain

This directory contains the pool drain feature.

The drain is modeled as a floor-mounted pool node with round or square grate
styles, a configurable suction outlet, and optional flow visualization.
